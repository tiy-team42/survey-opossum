<%= form_for(@long_answer_question) do |f| %>
  <% if @long_answer_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@long_answer_question.errors.count, "error") %> prohibited this long_answer_question from being saved:</h2>

      <ul>
      <% @long_answer_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answer %><br>
    <%= f.text_area :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
