<%= form_for(@boolean_question) do |f| %>
  <% if @boolean_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boolean_question.errors.count, "error") %> prohibited this boolean_question from being saved:</h2>

      <ul>
      <% @boolean_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answer %><br>
    <%= f.check_box :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
